/* ================================
   MAIN INITIALIZATION
   ================================
   Starts the application by rendering the initial state.
*/
updateLayout(); // Initial layout update
render(); // Initial render
renderBatchList(); // Initial list render